import SubdomainNotFound from '@/components/subdomains/SubdomainNotFound';

export default function NotFound() {
  return <SubdomainNotFound message="This subdomain doesn't exist or is inactive" />;
}
